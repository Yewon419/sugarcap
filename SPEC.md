# 슈가캡 (SugarCap) — 스펙 v0.2 (승인 전)

> 당·카페인 줄이기 기록 앱. iOS 네이티브 SwiftUI. 서버 없음, 계정 없음.
> 이 문서가 SSOT. 결정은 §9 미결 항목이 닫힐 때마다 본문에 반영한다.
> 작성 2026-09-15, 개정 2026-09-19(캐릭터·비우기 컵, §9.3), 2026-09-21(플랫폼·배포·액센트, §9.4), 2026-09-22(호감도·해금·감소 목표, §9.5). 상태: **Phase 1(기록 루프) 완료, Phase 2 착수 가능.**
> 화면 흐름 SSOT = `design/screen-flow.json` (편집기 `design/screen-flow.html`).

---

## 0. 한 문장

"하루치 당과 카페인이 담긴 컵이 마실 때마다 줄어들고, 밤에 남은 만큼을 캐릭터 카인·로슈에게 먹여 친해지면서 지난주보다 덜 마시게 하는 앱."

## 1. 포지셔닝 (경쟁 실사 기준)

| | 지금, 카페인 | 카페잉 | CoffeeLog | HiCoffee | **슈가캡** |
|---|---|---|---|---|---|
| 중심 | 조회·비교·랭킹 | 체내 카페인 반감기 | 기록·꾸미기·지출 | 카페인 예산·수면 | **줄이기** |
| 당 | 보조 수치, 기준선 없음 | 없음 | 칼로리만 | 없음 | **기준선·목표·추이** |
| 메인 | 숫자 대시보드 | 숫자+24h 그래프 | 스티커 달력 | 숫자+대사 그래프 | **줄어드는 컵 + 캐릭터** |
| 위젯 | 없음 | 없음 | 있음 | 있음 | 있음 |
| 가격 | 무료 | 무료 | 평생 Pro | 구독/평생 | 연간+평생 |

차별점 3개: (1) 당에 기준선이 있는 유일한 국내 앱, (2) 기록할수록 줄어드는 컵과 남은 양을 먹이는 캐릭터, (3) 기준을 서서히 낮추는 감소 목표와 지난주 대비.
기능 수로는 지금카페인(AI 추천·지도·1,100메뉴)을 못 이기므로 **기능을 늘리지 않는다.**

## 2. 데이터 (게이트 2 실측, 2026-09-15)

8개 브랜드 전부 공식 사이트에서 당류·카페인 공개, 전부 curl로 수집 가능.

| 브랜드 | 공개 단위 | 형태 | 특이 |
|---|---|---|---|
| 스타벅스 | Tall 355ml 1개 | 카테고리별 정적 JSON `/upload/json/menu/W000000N.js` | |
| 메가MGC | 온도별 591ml 1개 | `menu/menu.php?page=N` HTML 조각 | 단위 오타("g") 있음 |
| 컴포즈 | 591ml 1개 | 목록→상세 HTML | **당류 메뉴별 결측** |
| 이디야 | L/EX × HOT/ICED | `inc/ajax_brand.php` HTML 조각 17+페이지 | 추천 팝업 값은 플레이스홀더, 제외 |
| 빽다방 | HOT 510 / ICED 660ml | 정적 HTML | 가장 단순 |
| 투썸 | 레귤러/라지/맥스 × 온도 | `mo.twosome.co.kr` POST JSON 4개 + 상세 HTML | www에는 메뉴 없음. 온도 탭이 HTML에 **두 번** 렌더됨(중복 제거 필수) |
| 할리스 | Regular 354ml, HOT/ICED | 정적 HTML `/menu/espresso.do` 등 | |
| 더벤티 | 기준 사이즈(대개 라지 600ml) | 목록(`all.html?mode=N`)→상세 HTML | **카페인 원두별 2값(시그니처/다크)**, 사이즈 표기 미디엄/미디움 혼용 |

수집 결과(2026-09-16 실측)는 §10 게이트 기록 표를 본다. 브랜드별 파서는
`tools/scrape/sugarcap_scrape/brands/<id>.py`, 산출 스키마 계약은 `data/SCHEMA.md`.

### 2.1 카탈로그 모델 (불변 JSON, 앱 번들 + 원격 갱신)

필드 단위 정의와 불변식은 `data/SCHEMA.md`가 SSOT. 요약:

```
Brand      { id, name, servingNote, hasSizeChoice }
Drink      { id, brandId, name, nameEn?, category, temperature: hot|iced|both, servings[] }
Serving    { id, sizeLabel, volumeMl: Int?, sugarG: Double?, caffeineMg: Double?,
             caffeineVariants: [{label, mg}] }   // 더벤티 원두별
```

규칙:
- 사이즈별 데이터가 있는 브랜드(이디야·투썸)만 사이즈 선택 UI를 켠다. 나머지는 Serving 1개.
- **ml 환산 추정 금지.** 원두량이 용량에 비례하지 않는다. 브랜드가 공개한 값만 쓴다.
- `sugarG == nil`, `caffeineMg == nil`은 둘 다 "브랜드 미공개"다. 합계에는 0으로 더하되 표시는 "미공개"로 남긴다(§9.2 2026-09-16 결정).
- 카탈로그 ID는 `brand.slug + drink.slug + temperature + size`로 결정적 생성. 정의는 `ids.py` 한 곳. 갱신 시 기록이 끊기지 않게 ID 안정성 유지.

### 2.2 사용자 데이터 (SwiftData, 온디바이스)

```
Entry      { id, servingId, variantLabel?, quantity: Int, loggedAt: Date, sugarG, caffeineMg }  // 값 스냅샷
Favorite   { servingId, order }
Settings   { sugarLimitG, caffeineLimitMg, reductionGoal?, dayBoundaryHour, closeFromHour }
DaySettlement { day, closedAt: Date?, sugarLeftAtCloseG?, caffeineLeftAtCloseMg?,
                finalSugarLeftG?, finalCaffeineLeftMg?, finalizedAt: Date? }
Affinity   { character: kain|roshu, points: Int, level: Int }
```
Entry는 기록 시점 값을 스냅샷한다. 카탈로그 갱신으로 과거 기록이 바뀌면 안 된다.

- 남은 양 = `max(0, 하루 기준 − 그날 합계)`. 당 → 로슈, 카페인 → 카인.
- `DaySettlement`는 하루(경계 시각 기준)마다 1행. 마감 시점 값(`…AtClose`)은 연출용, 호감도는 **하루 경계 시각의 최종 값(`final…`)으로만** 적립한다(§4.7).
- `Affinity.points`는 늘기만 한다. 기준 초과로 깎이지 않는다. 적립 공식은 §9-4.
- **호감도는 먹이기 동작(마감 또는 다음 날 먹이기)으로만 적립한다.** 자동 정산 없음. 앱을 열지 않은 날은 다음 날 한 번만 묻는다(§4.7).

### 2.3 갱신
- 수집 스크립트(Python, `tools/scrape/`) → `catalog.json` + 검증(`validate.py`: ID 유일성·ID 규칙 일치·브랜드별 최소 메뉴 수·이상치) → 리포 커밋. 검증 실패 시 파일을 쓰지 않고 exit 1.
- 앱은 번들 JSON을 기본으로 쓰고, 원격 JSON이 더 최신(`built_at`)이면 교체. **서버 로직 없음 — 정적 파일 1개만 호스팅한다.**
- 원격 배포처는 **Vercel 정적 배포**(2026-09-21 확정, §9.4). 리포가 private이라 GitHub raw URL은 인증 없이 열리지 않는다(실측 404). 앱은 캐시를 Application Support에 두고, 네트워크·파싱 실패 시 조용히 번들 JSON으로 폴백한다.

## 3. 기준선

| 항목 | 기본값 | 근거 | 사용자 조정 |
|---|---|---|---|
| 카페인 | 400 mg/일 | 식약처 성인 1일 최대 섭취 권고 | 100~600 슬라이더 |
| 당 | 50 g/일 | WHO 유리당 1일 상한(총열량 10%, 2,000kcal 기준) | 프리셋 25 / 50 / 100 |

두 출처 링크는 Phase 1 착수 전 원문 확인 후 여기 박는다(§9-3).

## 4. 화면 (레퍼런스 8장 대조 후 설계)

레퍼런스에서 채택한 골격: 오늘 화면 = 큰 수치 + 기준선 막대 + 하단 빠른 기록 행(WaterMinder·카페잉), 추가 = 시트 + 브랜드 칩 + 검색 + 최근 + 하단 사이즈·미리보기(HiCoffee·카페잉). 스티커·지도·AI 추천은 채택 안 함.

탭 3개: **오늘 / 추이 / 설정.** 탭 간 슬라이드 없음, 각 탭 자기 스택.

### 4.1 오늘 (루트)
- 상단: 컵 1개. 메인은 당 컵 + 로슈, 좌우 스와이프로 카페인 컵 + 카인. 캐릭터는 컵에 붙어 있고 탭하면 반응 동작.
- 컵은 **하루 시작 때 가득 차 있고 기록할 때마다 줄어든다.** 액체 높이 = 남은 양 / 하루 기준(§2.2). 줄어드는 애니메이션(스프링, 400ms) + 햅틱 1회.
- 기준 초과: 빈 컵 + 넘긴 양(`+12 g`) 표시, 캐릭터가 가볍게 아쉬워하는 동작 1종. 호감도는 깎지 않고 죄책감 문구 없음.
- 컵 아래: `남은 18 g / 50 g` 식 수치, tabular numerals.
- 하단: 오늘 기록 리스트(시간·음료·당·카페인), 스와이프 삭제, 롱프레스 컨텍스트 메뉴(즐겨찾기·수량 변경).
- 컵 아래 **브랜드 선택**(8개 + "직접 입력"). 브랜드 탭 → 4.2 브랜드 메뉴(푸시). 음료 추가 시트·"전체" 보기 없음.
- "오늘 마감" 버튼: 마감 가능 시각(기본 저녁 8시) 이후에만 노출 → 4.7.
- 다음 날 첫 실행 배너: 전날 미마감이면 "어제 남은 음료 먹이기"(→ 4.7), 전날 앱을 안 열었으면 "음료를 안 마셨나요?"(→ 4.7), 마감 뒤 기록이 있었으면 "어젯밤 이후 마신 만큼 빠졌어요" 한 줄.
- 캐릭터 프로필 버튼 → 4.8 호감도(Pro), 무료는 페이월.
- 빈 상태: 컵 가득, 캐릭터 대기 동작, "첫 잔을 기록해 보세요" 한 줄. 스켈레톤 없음(로컬 데이터).

### 4.2 브랜드 메뉴 (오늘 스택에 푸시)
- 상단: 브랜드명 + 그 브랜드 안 검색창(자동 포커스 안 함).
- 목록: 그 브랜드 메뉴 전체. 행 = 이름 / 당 g · 카페인 mg / 사이즈 라벨.
- 행 탭 → 하단 고정 패널: 사이즈 세그먼트(해당 브랜드만)·원두 세그먼트(더벤티만)·수량 스테퍼·미리보기(당 g, 카페인 mg 크게)·"추가" 버튼.
- 추가 → 오늘 루트로 복귀 → 컵이 줄어듦.
- 검색 0건: "직접 입력" 행 노출(이름·당·카페인 수동, 시트).

### 4.3 추이
- 주간 막대 2개 세트(당·카페인), 7일. 상단 세그먼트 주/월.
- "지난주 대비 −18%" 한 줄. 스트릭·죄책감 문구 없음.
- 감소 목표(Pro): 하루 기준을 주 또는 월 단위로 조금씩 낮추는 계획. 세부 규칙은 §9-8.

### 4.4 설정
- 하루 기준 2개(무료), 하루 경계 시각, 마감 가능 시각(기본 20시), 즐겨찾기 정렬, Pro 구매·복원, 데이터 갱신 날짜, 정보·개인정보(수집 없음).

### 4.5 온보딩
- 1화면: 앱 설명 한 줄 + 기준선 기본값 안내 + "시작". 계정 없음. 완료 후 `replace`로 루트 진입, 뒤로 가기 불가.

### 4.6 위젯 (WidgetKit)
- Small: 남은 양 컵 2개 + 수치 + 캐릭터 정지 포즈. 위젯은 애니메이션 불가라 포즈 1장. Medium: 같은 구성 + 즐겨찾기 3개 원탭 기록(AppIntent, iOS 17).
- v1은 Small만. Medium 인터랙티브는 v1.1.

### 4.7 마감·먹이기 (fullScreenCover)
- 진입: 오늘 화면 "오늘 마감"(마감 가능 시각 ~ 하루 경계 전) 또는 다음 날 첫 실행 배너(전날 미마감).
- 연출: 남은 당은 로슈에게, 남은 카페인은 카인에게 먹이는 동작. 단계가 오르면 상승 연출 + 새 동작·표정 해금(무료는 한도 단계까지, §6).
- **이른 마감 방지:** 마감 버튼은 마감 가능 시각 이후에만 열린다. 마감 뒤에도 기록할 수 있고, 그 음료는 그날 몫으로 빠진다. 호감도는 하루 경계 시각의 최종 남은 양으로 확정한다(§2.2). 마감 시점 연출보다 줄었으면 다음 날 첫 실행에 한 줄로 알린다.
- 미마감: 날리지 않는다. 다음 날 첫 실행에 "어제 남은 음료 먹이기"로 같은 화면을 연다(최종 값 기준). 제안은 바로 다음 날까지만, 그 뒤엔 적립 없이 넘어간다.
- 앱을 안 연 날: 안 마셔서 안 열었을 수 있으므로 다음 날 **한 번만** "어제는 기록이 없어요. 음료를 안 마셨나요?"를 묻는다. "안 마셨어요" → 가득 찬 컵으로 먹이기. "마셨어요" → 적립 없이 닫고 다시 묻지 않는다. 전날(바로 어제)만 묻고, 여러 날 비었으면 그 이전 날은 묻지 않는다. 설치 전 날은 묻지 않는다.
- "호감도 자세히" → 4.8(Pro), 무료는 페이월.

### 4.8 호감도 (Pro)
- 카인 / 로슈 전환, 호감도 수치·기록, 단계별 해금 동작·표정 목록과 미리보기.
- 무료 사용자는 이 화면 대신 페이월. 무료도 적립·단계 상승·한도 내 해금은 받는다.

## 5. 디자인 토큰 (앱 전체 락)
- 액센트 1색(CTA·활성·진행). 컵 채움 색 2개는 데이터 색이며 액센트가 아니다. **액센트 = 딥 틸 `#0F7A73`, 2026-09-21 확정.** 다크 변형은 라이트 전용 결정으로 삭제(2026-09-23). 컵 액체색(로슈 `#f3a6b8`, 카인 `#a07350`)과 겹치지 않는 색에서 골랐다. 에셋 카탈로그 `AccentColor`가 유일한 정의 지점.
- 그레이 1계열(웜). 서피스는 시맨틱 컬러.
- **라이트 모드 전용**(2026-09-22 확정). `Info.plist` `UIUserInterfaceStyle = Light`로 고정해 시스템이 다크여도 앱은 라이트로 그린다.
- 라디우스: 버튼 pill, 카드 16, 입력 8, 칩 pill. 연속 코너.
- 아이콘 SF Symbols만. 크롬에 이모지 0.
- 타이포: 시스템 ramp, Large Title은 오늘 화면만. 숫자는 tabular.
- 라벨 통일: 추가 = "추가", 기록 = "기록", 기준선 = "하루 기준", 마감 = "오늘 마감".
- **캐릭터는 v1에서 정지 스프라이트 1장씩이다**(2026-09-21 확정, §9.4). 탭 반응은 이미지 교체가 아니라 SwiftUI 스케일·스프링으로 낸다. 아래 연속 이미지 규격은 프레임 원화가 나온 뒤에 적용한다.
- 캐릭터 애니메이션(추후) = **PNG 연속 이미지**(알파 채널 유지). GIF는 쓰지 않는다(1비트 투명도로 컵 위 가장자리가 깨지고 256색 한계, SwiftUI 기본 재생 미지원). 동작별 시퀀스(대기·반응·아쉬움·먹기·단계 상승)를 따로 둔다.

## 6. 과금 (StoreKit 2)
- 무료: 기록, 컵 화면, 브랜드 8개 전체, 최근·즐겨찾기, 하루 기준(목표) 지정, 마감·먹이기, 호감도 적립·단계 상승 연출, **한도 단계까지의 동작·표정 해금**(한도는 §9-5).
- Pro: 호감도 확인(4.8), 한도 이후 단계 해금, 감소 목표(주간·월간), 추이 월 보기, 지난주 대비, 위젯, 컵 테마, iCloud 동기화(v1.1).
- 가격: 연간 ₩9,900대, 평생 ₩29,000대(연간의 3배). 월간 없음. 가격 결정은 §9.1.
- 페이월은 Pro 기능을 탭한 자리에서 시트로 열고, 구매 후 그 화면으로 돌아온다. 온보딩에 페이월 없음.

## 7. 범위 밖 (v1에 넣지 않음)
계정·서버·AI 추천·지도·사진·지출·HealthKit 쓰기·Apple Watch·Android·편의점 음료·에너지드링크.
(HealthKit `dietaryCaffeine`/`dietarySugar` 쓰기는 v1.1 후보.)

## 8. 구현 Phase (각 Phase 끝에 검증·승인 게이트)

| Phase | 산출 | 게이트 |
|---|---|---|
| 0 데이터 | `tools/scrape/` 8개 브랜드 → `catalog.json`, 검증 스크립트, 스키마 문서 | mypy·ruff 통과, 브랜드별 메뉴 수·결측률 표 |
| 1 기록 루프 | XcodeGen 프로젝트, SwiftData, 카탈로그 로더, 오늘 화면, 음료 추가 시트, TestFlight CI | 시뮬레이터 라이트·Dynamic Type XL 스크린샷, 플로우 녹화 (다크는 §5 라이트 전용 결정으로 제외) |
| 2 추이·설정·캐릭터 | 추이 탭, 기준선·하루 경계·마감 시각, 온보딩, 마감·먹이기, 호감도 적립, 캐릭터 PNG 시퀀스 재생 | 동일 + 경계 시각 전후 정산 시나리오 테스트 |
| 3 위젯·Pro | Small 위젯, StoreKit 2, 페이월 시트 | 샌드박스 구매·복원 확인 |
| 4 출시 | 아이콘·스크린샷·설명·개인정보 문항, 심사 제출 | 심사 통과 |

스택: Swift 5.10+, iOS 17+, SwiftUI, SwiftData, WidgetKit, StoreKit 2, XcodeGen, GitHub Actions macOS 러너 → TestFlight. 데이터 툴은 Python 3.13 + httpx + pydantic + bs4, mypy strict, ruff. 위치 `tools/scrape/`, 산출 `data/catalog.json`.

## 9. 미결

1. **KIPRIS 상표 검색(9류·42류 "슈가캡")** — 사용자 직접 확인 대기. App Store 국내 0건, 웹 0건은 확인됨.
2. 영문 표기 `SugarCap` 스토어 등록명 병기 여부. 홈 화면 표시명은 **`슈가캡` 확정**(2026-09-21, §9.4).
3. 기준선 출처 원문 링크 확인(식약처 400mg, WHO 50g). 기본값 자체는 확정(§3).
4. 호감도 적립 공식 → **확정(2026-09-22), §9.5.**
5. 무료 해금 한도 → **확정(2026-09-22), §9.5.**
6. 캐릭터 디자인·프레임 제작 방식(시안 도구, 프레임 수, 해상도).
7. 오늘 화면 "빠른 기록" 행 → **확정(2026-09-21): 넣지 않는다.** 흐름도(`screen-flow.json`)와 `prototype.html` 둘 다 이미 빠져 있었고 본문 §4.1만 남아 있었다. §4.1에서 삭제했다. 원탭 기록은 즐겨찾기 모델·정렬 UI를 함께 끌고 오므로 v1 범위에서 뺀다.
8. 감소 목표 세부 규칙 → **확정(2026-09-22), §9.5.**
9. 컵 커스터마이징 = 컵 종류 + 내용물 + 배경 전체 교체(2026-09-20). 남은 것: §6 "컵 테마"(Pro)와 같은 기능인지, 무료·Pro 경계, 세트 수.
10. 컵 에셋 규격 → **확정(2026-09-20): 단계 이미지 방식.** 컵은 AI 생성 이미지(Blender 중단). 레이어 분리 대신 세트마다 배경·컵·액체가 다 들어간 **완성 장면을 채움 단계별로** 둔다(샘플: 아이스 아메리카노, 2048px). 앱은 남은 비율을 단계로 내림해 해당 장면을 표시하고 단계 전환은 크로스페이드. 남은 양이 0보다 크면 최소 첫 단계(0% 장면은 정확히 0일 때만). **단계는 9장: 0·10·20·30·40·50·70·80·100%**(60·90은 수위가 인접 단계와 구분되지 않아 제외, 2026-09-20). 간격이 고르지 않으니 매핑은 "남은 비율 이하 중 가장 높은 단계"(예: 95% → 80, 65% → 50). 100% 장면은 한 잔도 안 마셨을 때만. 화면 비율 9:16, 창은 100% 크롭(937×1666) 기준이고 나머지 단계는 컵 윤곽(벽·림)을 재서 100%에 정렬한다. 샘플 세트 `design/assets/cups/iced-americano/`(`original/`, `9x16/`).
   - **애니메이션 확정(2026-09-20):** 단계마다 대기 루프(얼음 흔들림 등) + 인접 단계 사이 전환 클립. 세트당 루프 9 + 전환 8. 여러 단계를 한 번에 내려가면 인접 전환을 이어 재생. 기록 삭제로 차오를 때는 크로스페이드.
   - **포맷: HEVC MP4, 약 10fps.** GIF 기각. 실측(50→40% 전환 2초, 10fps, 1024px): GIF 7,812KB / H.264 119KB / HEVC 47KB. 원작업은 GIF·MP4 무엇이든 받고, 앱 번들용 HEVC 변환은 스크립트로 한다.
   - **이음새 규칙:** 전환 클립의 첫 프레임 = 출발 단계 정지 장면, 마지막 프레임 = 도착 단계 정지 장면. 루프는 첫 프레임 = 끝 프레임.
   - 남은 것: 100% 재생성(현재 100%만 컵이 약 9% 커서 나머지를 1.09배 키워 맞춘 상태. 80% 이미지를 참조로 "가득 채우기" 편집 권장), 번들 해상도.

### 9.1 확정 (2026-09-15 승인)
- 과금 경계 §6대로. 브랜드 8개 전체 무료.
- 가격: 연간 9,900원대, 평생 29,000원대, 월간 없음. 스토어 등록 시 정확한 티어 선택.
- 기준선: 당 50g, 카페인 400mg, 프리셋 25/50/100.
- 하루 경계 기본값 **새벽 4시**, 설정에서 변경 가능.
- 컴포즈 당류 결측: 합계 0 + "미공개" 표시.
- 위젯 v1 Small만.
- 데이터 툴 Python은 3.13(로컬 `py -3.13`), 3.12 표기는 §8에서 정정.

### 9.2 확정 (2026-09-16 승인)
- **카페인 미공개 행을 드롭하지 않는다.** `Serving.caffeineMg`를 nullable로 두고 카탈로그에 남긴다. 드롭 규칙을 적용하면 295 servings(주스·에이드·스무디·버블티 등 당류가 높은 논커피 메뉴)가 통째로 빠지는데, 그 행들은 당류 값을 갖고 있다. 당 기록이 이 앱의 본체이므로 카페인 미공개를 이유로 버리지 않는다.
- 앱 표시 규칙: 합계에는 0으로 더하고 해당 음료에는 "미공개"를 남긴다. 당류 결측과 동일 규칙.

### 9.3 확정 (2026-09-19 승인)
- **캐릭터 2명: 카인(카페인 컵), 로슈(로우슈거, 당 컵).** 컵에 붙어 있는 애니메이션.
- **컵은 채우지 않고 비운다.** 가득 찬 채로 시작해 기록할 때마다 줄어든다. 채움 방식은 "더 마셔야 채워진다"로 읽혀 앱 목적과 반대 신호라 폐기.
- 오늘 화면 컵 1개 + 좌우 스와이프(2026-09-16 디자인 구상 반영). 기존 "컵 2개 나란히" 대체.
- 하루 끝에 남은 양을 캐릭터에게 먹여 호감도 적립. 먹이기는 **밤에 사용자가 직접 마감**, 규칙은 §4.7(마감 가능 시각 20시, 마감 후 기록도 차감, 경계 시각에 확정, 미마감은 다음 날 먹이기).
- 기준 초과: 캐릭터 가벼운 반응만. 호감도 감소 없음.
- 캐릭터 원화 확정(2026-09-20, 대표님 작업). 카인 = 갈색 새, 외곽선 없음, 옆모습. 로슈 = 회청색 동글이, 외곽선, 정면. **그림체·시점 차이는 의도.** 애니메이션 프레임도 대표님이 직접 제작.
- 컵 액체 색은 캐릭터 색과 무관. 내용물은 커스터마이징 기능으로 바꾼다(세부 §9-9).
- 음료 추가 시트 폐기(2026-09-20). 오늘 화면에 브랜드 선택을 바로 두고 브랜드 메뉴로 푸시(§4.1·4.2). "전체" 보기 없음.
- 호감도는 먹이기를 해야만 쌓인다(§2.2). 기록 0잔인 날도 먹이면 남은 양대로 적립한다. 자기 보고 앱이라 미기록은 막지 않는다. 앱을 안 연 날은 다음 날 한 번 "안 마셨나요?"로 묻는다(§4.7).
- 애니메이션은 PNG 연속 이미지. GIF 기각(§5).
- 과금: 먹이기·하루 목표는 무료. 호감도 확인·감소 목표는 Pro. 동작·표정 해금은 무료 한도 단계까지, 이후 Pro.
- 카페인 미공개 음료(§9.2)는 카인 컵을 줄이지 않는다. 기존 표시 규칙("미공개") 그대로 적용.

### 9.4 확정 (2026-09-21 승인)
- **플랫폼은 네이티브 SwiftUI 유지.** 웹앱(Vercel/Next.js) 전환을 검토했으나 기각 — WidgetKit 위젯(§1 차별점 표)과 StoreKit 2(§6 수익 모델 전체)를 잃고, 서버를 두면 §7 "계정·서버 범위 밖"과 §4.4 "수집 없음" 포지셔닝이 동시에 무너진다. 데이터 갱신은 네이티브에서도 심사 없이 된다 — 데이터는 코드가 아니다.
- **카탈로그 원격 배포는 Vercel 정적 호스팅**(§2.3). 리포가 private이라 GitHub raw URL이 인증 없이 안 열리는 게 결정 근거(실측 404). 서버 함수·DB·계정 없음.
- **"실시간 갱신"의 실제 주기는 시즌 신메뉴 단위다.** 수집이 8개 사이트 전수 크롤링(전체 약 12분)이라 초·분 단위는 구조적으로 불가능하고, 필요하지도 않다.
- **Bundle ID `com.sugarcap.app`.** App Store Connect 앱 레코드에 영구 고정된다.
- **홈 화면 표시명 `슈가캡`**(한글). 스토어 등록명 영문 병기는 §9-2로 남긴다.
- **액센트 `#0F7A73`**, §5.
- **캐릭터는 v1에서 정지 스프라이트 1장씩**(§5). 애니메이션 프레임 원화가 나오기 전까지 탭 반응은 SwiftUI 스케일로 낸다. 컵은 §9-10대로 9단계 정지 이미지 + 크로스페이드를 그대로 쓴다.
- **빠른 기록 행 제외**(§9-7).
- **Phase 1 범위 축소 확인:** 추이·설정·온보딩·마감/먹이기·위젯·StoreKit은 Phase 1에 넣지 않는다. §9-4(호감도 공식)·§9-5(무료 해금 한도)·§9-8(감소 목표)이 미결이라 Phase 2·3을 열 수 없다.
- **CI 제약:** 개발 환경(Windows)에 Xcode가 없어 **Swift 컴파일 검증 경로가 CI 하나뿐이다.** private 리포의 macOS 러너는 분당 10배 과금(무료 2,000분 = macOS 실질 200분)이라, 푸시에는 서명 없는 빌드만 돌리고 TestFlight 업로드는 수동 실행으로 뺐다(`docs/RELEASE.md`).

### 9.5 확정 (2026-09-22 승인)
- **호감도 적립(§9-4):** 먹이기 1회에 캐릭터별로 `1 + round(남은 양 / 하루 기준 × 9)`점, 하루 1~10점. 남은 당 → 로슈, 남은 카페인 → 카인으로 따로 적립한다. 비율은 0~1로 자른다. 기준을 넘긴 날(남은 0)도 먹이면 1점이다. 남은 양은 §2.2대로 하루 경계 시각의 최종 값을 쓴다.
- **단계 10개(§9-4):** 누적 points 기준 Lv1 0 / Lv2 30 / Lv3 90 / Lv4 180 / Lv5 300 / Lv6 450 / Lv7 630 / Lv8 840 / Lv9 1080 / Lv10 1350(Lv n = 15·n·(n−1)). 하루 평균 5점이면 Lv2 6일, Lv3 18일, Lv5 60일, Lv10 270일.
- **무료 해금 한도(§9-5):** 해금 단위는 단계마다 **표정 정지 스프라이트 1장**(v1은 정지 스프라이트 구조, §5). 무료는 Lv3까지 해금한다. Lv4 이상은 적립·단계 상승 연출을 무료에도 그대로 보여 주되 새 표정은 Pro로 잠근다. Pro 구매 시 이미 도달한 단계의 표정은 즉시 풀린다. 필요 원화 = 캐릭터당 기본 1 + 해금 9장.
- **감소 목표(§9-8, Pro):** 사용자가 목표치와 기간(4·8·12주)을 고른다. 주당 낮추는 폭 = (시작 기준 − 목표) / 주 수. 해당 주의 하루 기준 = 시작 기준 − 폭 × (달성한 주 수)이며 당 5g, 카페인 25mg 단위로 반올림한다. 주는 목표 시작일부터 7일(하루 경계 시각 기준)이다.
  - 판정: 그 주 7일 중 **5일 이상** 하루 합계가 그 주 기준 이하면 달성. 기록 0인 날도 기준 이하로 센다(자기 보고 앱, §9.3).
  - 미달이면 다음 주 기준을 낮추지 않고 유지한다. 벌점·죄책감 문구 없음. 그만큼 계획 기간이 늘어난다.
  - 목표치에 도달하면 계획을 끝내고 목표치를 하루 기준으로 유지한다.

## 10. 게이트 기록
- 2026-09-15 게이트 1: 지금카페인 스크린샷 4장 대조. 당 기준선·목표·위젯·컵 비주얼 전부 없음 확인.
- 2026-09-15 게이트 2: 브랜드 8개 공식 사이트 실측. 표는 §2.
- 2026-09-15 이름: 후보 7개 App Store 검색. 슈가캡 확정(반컵·당끊·컵로그 탈락). KIPRIS 미확인.
- 2026-09-16 **Phase 0 완료**. 8개 브랜드 파서 + `catalog.json` + `validate.py` + `data/SCHEMA.md`. 게이트: ruff·mypy 0 에러, pytest 98건 통과(live 17건 포함, 실사이트 호출).

| 브랜드 | drinks | servings | 당류 미공개 | 카페인 미공개 | 원두별 변형 |
|---|---|---|---|---|---|
| 스타벅스 | 194 | 194 | 2 | 2 | 0 |
| 메가MGC | 158 | 158 | 0 | 0 | 0 |
| 컴포즈 | 164 | 164 | 11 | 69 | 0 |
| 이디야 | 135 | 266 | 0 | 0 | 0 |
| 빽다방 | 296 | 296 | 8 | 24 | 0 |
| 투썸 | 118 | 262 | 0 | 98 | 0 |
| 할리스 | 114 | 114 | 0 | 40 | 0 |
| 더벤티 | 129 | 129 | 0 | 62 | 11 |
| **합계** | **1,308** | **1,583** | **21** | **295** | **11** |

결측률: 당류 1.3%, 카페인 18.6%. 카페인 결측은 대부분 논커피(주스·에이드·스무디·티)이며 §9.2에 따라 보관한다.
